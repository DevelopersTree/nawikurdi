import * as index from './index';
import * as favorites from './favorites';
import * as submit_name from './submit-name';

window.nawikurdi = {
    index: index,
    favorites: favorites,
    submit_name: submit_name
}